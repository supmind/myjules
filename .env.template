# 这是一个 .env 文件的模板。
# 请复制此文件为 .env，然后填入您的真实凭据。
# 确保 .env 文件已被添加到 .gitignore 中，以防意外提交。

# Autogen LLM 配置
# 将下面的 JSON 字符串取消注释并填入您的模型配置。
# 例如，对于 OpenAI:
# OAI_CONFIG_LIST='[{"model": "gpt-4", "api_key": "sk-..."}]'
#
# 对于本地模型 (LM Studio):
# OAI_CONFIG_LIST='[{"model": "lmstudio-community/Meta-Llama-3-8B-Instruct-GGUF", "api_key": "lm-studio", "base_url": "http://localhost:1234/v1"}]'

OAI_CONFIG_LIST=