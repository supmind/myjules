# 这是一个 .env 文件的模板。
# 请将此文件复制为 .env，并填入您自己的 API 密钥和配置。

# --- LLM 配置 ---
# 这是一个 JSON 格式的数组，用于定义 AutoGen 可以使用的模型。
# 您可以定义多个模型配置。默认情况下，应用程序会使用列表中的第一个配置。
# AutoGen 的 OpenAIChatCompletionClient 本身就支持任何与 OpenAI API 兼容的端点，包括 Google Gemini。
# 您只需要提供正确的 "base_url" 和模型名称即可。

# 示例 1: 配置一个标准的 OpenAI 兼容 API (例如 DeepSeek)
# OAI_CONFIG_LIST='[{"model": "deepseek-coder", "api_key": "YOUR_DEEPSEEK_API_KEY", "base_url": "https://api.deepseek.com/v1"}]'

# 示例 2: 配置 Google Gemini (使用其 OpenAI 兼容端点)
# 注意：模型名称通常需要以 "models/" 开头。
# OAI_CONFIG_LIST='[{"model": "models/gemini-1.5-pro-latest", "api_key": "YOUR_GEMINI_API_KEY", "base_url": "https://generativelanguage.googleapis.com/v1beta"}]'

# 示例 3: 将多个配置组合在一起
# 应用程序会使用列表中的第一个配置。
OAI_CONFIG_LIST='[{"model": "models/gemini-1.5-pro-latest", "api_key": "YOUR_GEMINI_API_KEY", "base_url": "https://generativelanguage.googleapis.com/v1beta"}, {"model": "deepseek-coder", "api_key": "YOUR_DEEPSEEK_API_KEY", "base_url": "https://api.deepseek.com/v1"}]'

# --- Git 配置 (可选) ---
# 这些变量用于在使用 git 工具时设置提交的作者信息。
# GIT_AUTHOR_NAME="Your Name"
# GIT_AUTHOR_EMAIL="your.email@example.com"